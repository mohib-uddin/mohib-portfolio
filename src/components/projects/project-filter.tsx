"use client";
import {useState} from "react";

interface filterProps{
    filterItem:string;
}
const ProjectFilter=({filterItem}:filterProps)=>{
    const [projects,setProjects]=useState();
}
export default ProjectFilter;
